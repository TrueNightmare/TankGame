using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CodedSound {

    /*
     * Place Anyway and change "PlayerDeath" to clip name
     * AudioManager MUST! be in scene or it won't work!
    */
	//FindObjectOfType<AudioManager>().Play("PlayerDeath");
}
